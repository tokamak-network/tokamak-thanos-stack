apiVersion: v1
kind: Service
metadata:
  name: {{ include "drb-node.fullname" . }}
  labels:
    {{- include "drb-node.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "drb-node.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 3000
      targetPort: 3000
