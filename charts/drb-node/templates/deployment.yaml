apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "drb-node.fullname" . }}
  labels:
    {{- include "drb-node.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "drb-node.selectorLabels" . | nindent 6 }}
  replicas: {{ .Values.cross_trade.spec.replicas }}
  template:
    metadata:
      labels:
        {{- include "drb-node.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}

    spec:
      terminationGracePeriodSeconds: 10
      restartPolicy: Always
      containers:
        - name: bridge
          image: "tokamaknetwork/drb-node-app:latest"
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
          volumeMounts:
            - name: app-config
              mountPath: "/app/.env"
              subPath: .env
              readOnly: true
      volumes:
        - name: app-config 
          configMap:
            defaultMode: 0777
            items:
              - key: .env
                path: .env
            name: {{ include "drb-node.fullname" . }}
